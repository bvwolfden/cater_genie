# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env*.local

# data / generated
/scripts/.pgdata
/prisma/seed-data/*.json
/src/generated

# client data — NEVER commit (real payroll, sales, balances, PII).
# Repo is public; the spreadsheets + their screenshot stay local only.
*.xlsx
/docs/dashboard.png
/drop

# typescript incremental build state
tsconfig.tsbuildinfo

# misc
.DS_Store
*.pem
npm-debug.log*
.vscode/*
!.vscode/extensions.json
